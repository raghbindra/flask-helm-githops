name: Build and push Docker image

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set SHORT_SHA env
        run: |
          echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_ENV
          echo "SEMVER_TAG=v0.0.${{ github.run_number }}" >> $GITHUB_ENV

      - name: Build, tag and push
        run: |
          IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/custom-flask-app"
          docker build -f app1/Dockerfile -t "${IMAGE}:${{ github.sha }}" app1
          docker tag "${IMAGE}:${{ github.sha }}" "${IMAGE}:${{ env.SHORT_SHA }}"
          docker tag "${IMAGE}:${{ github.sha }}" "${IMAGE}:latest"
          docker push "${IMAGE}:${{ github.sha }}"
          docker push "${IMAGE}:${{ env.SHORT_SHA }}"
          docker push "${IMAGE}:${{ env.SEMVER_TAG }}"
          docker push "${IMAGE}:latest"